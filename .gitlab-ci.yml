stages:
#  - build
#  - test
  - danger

before_script: 
  - bundle install

#before_script:
#  - bundle install

#build_project:
#  stage: build
#  script:
#    - xcodebuild clean -project iOS-Boilerplate.xcodeproj -scheme iOS-Boilerplate | xcpretty
#  tags:
#    - xcode

#fastlane_test:
#  stage: test
#  script:
#    - bundle exec fastlane tests
#  tags:
#    - xcode

danger_zone:
  stage: danger
  script:
    - bundle exec danger

variables:
  GIT_SSL_NO_VERIFY: "1"
  GIT_SUBMODULE_STRATEGY: recursive
